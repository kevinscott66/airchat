# AirChat

[English](README.md) · [Русский](README.ru.md)

[![CI](https://github.com/kevinscott66/airchat/actions/workflows/ci.yml/badge.svg)](https://github.com/kevinscott66/airchat/actions/workflows/ci.yml) · [MIT](LICENSE)

Мессенджер без номера телефона и регистрации. Ваша seed-фраза — ваш аккаунт: одна идентичность открывает зашифрованную историю на другом устройстве.

**Статус:** Активная разработка · web доступен. Текущее состояние: сентябрь 2026.

[ Подробный кейс ](https://dobropalm.tech/ru/case-studies/airchat/) · [Портфолио](https://dobropalm.tech) · [Открыть продукт](https://air.dobropalm.tech)

![Реальный экран публичного web-клиента в чистой сессии. Аккаунт не создавался; личные сообщения и seed-фразы отсутствуют.](https://dobropalm.tech/assets/media/airchat.webp)

_Реальный экран публичного web-клиента в чистой сессии. Аккаунт не создавался; личные сообщения и seed-фразы отсутствуют._

## Задача и результат

Смена телефона часто означает зависимость от номера, оператора аккаунта или отдельного резервного архива. Задача — дать человеку переносимый аккаунт и синхронизацию, не передавая серверу ключи от содержимого.

Доступен браузерный клиент. В исходниках реализованы переносимая идентичность, зашифрованная синхронизация аккаунта, ревизии, удаление через tombstone и проверка принадлежности профилю. Нативные сборки дополняют web локальным LAN-обменом. Демо ниже показывает реальный экран входа, а не испытание всей криптографической модели.

## Мой вклад

Я определяю поведение аккаунта и восстановления, пользовательские сценарии, границы доверия между клиентом и сервером и конструкцию синхронизации. Отвечаю за то, чтобы техническая модель оставалась понятной человеку: что защищено, что видно серверу и что происходит при потере ключа.

В разработке использую AI-инструменты; продуктовые и архитектурные решения принимаю сам.

## Engineering highlights

- **Повтор запроса без повтора изменения.** Идемпотентные мутации и курсоры позволяют повторно доставить данные после обрыва связи. Подписанный запрос и его nonce решают отдельную задачу — защиту от повторного воспроизведения.
- **Удаление тоже синхронизируется.** Tombstone сохраняет факт удаления. Иначе другой клиент может загрузить старую запись и вернуть уже удалённое сообщение.
- **История и соединение — разные сервисы.** Сигналинг помогает установить связь между устройствами. Он не становится второй базой сообщений.

## Архитектура и стек

| Слой | Реализация |
|---|---|
| Frontend | React Native, Expo 55, TypeScript; web и нативные клиенты |
| Backend | Node.js, cloud-vault / sync, отдельный WebRTC-сигналинг |
| Данные | SQLite на клиенте; зашифрованные сущности, ревизии и курсоры на сервере |
| Безопасность | BIP39, did:key, @noble; ключи и шифрование на клиенте |

У мутации есть идентификатор, ревизия, владелец и признак удаления. sync_entity_heads хранит локальные версии, sync_state — курсор и состояние синхронизации. Схема применяется на стороне соответствующего сервиса; это не одна общая база для всех клиентов.

## Quick start

```bash
# Requires Node.js 22.22+ (node:sqlite).
git clone https://github.com/kevinscott66/airchat.git
cd airchat/server/cloud-vault
npm ci
npm test
CLOUD_VAULT_DIR=/tmp/airchat-demo-data HOST=127.0.0.1 PORT=3010 npm start
```

Это запуск sync/vault-сервиса с отдельной локальной папкой данных. Для клиента вернитесь в корень, выполните `npm ci`, затем `npm run web` или платформенную сборку. Нативным модулям нужна development-сборка, Android SDK или Xcode; серверный Quick Start не запускает весь мессенджер.

## Проверки

```bash
# From the repository root
npm run typecheck
npm run lint
npm test -- --runInBand
npm run test:servers
npm run web:export
```

Бейдж выше показывает реальный workflow; перечень команд не означает, что все проверки выполнялись при каждой правке README.

## Развёртывание, мониторинг и API

Синхронизация, сигналинг и уведомления разделены на самостоятельные сервисы. В исходниках предусмотрены scrubber логов и Sentry. Запуск нативного клиента требует соответствующей сборки и платформенных зависимостей; web имеет собственные ограничения.

```bash
curl --fail http://127.0.0.1:3010/health
# {"ok":true,"service":"airchat-cloud-vault-example","release":null}
```

Синхронизация использует подписанные запросы. Push/pull, курсоры и регистрацию устройств см. в `server/cloud-vault/README.md`. Реальные seed-фразы не нужны для примеров API.

## Безопасность и ограничения

Сервер видит метаданные. Посты по публичной ссылке не зашифрованы. Защита ключей в браузере слабее нативного secure storage. Для синхронизации и звонков нужна связь. Независимый аудит криптографической системы здесь не заявляется.

Восстановление через seed-фразу убирает зависимость от номера телефона, но потерю фразы нельзя исправить сбросом пароля у оператора. Зашифрованные данные скрывают содержимое, но не все метаданные соединения и синхронизации.

Политика раскрытия уязвимостей: [SECURITY.md](SECURITY.md).

## История и документация

Репозиторий содержит клиент и серверные компоненты. Версии исходников не означают независимый аудит безопасности или доступность каждой нативной сборки.

- [Sync architecture](docs/sync-architecture.md)
- [Cloud-vault service](server/cloud-vault/index.js)
- [Client sync implementation](src/core)
- [Threat model](SECURITY.md)
- [Server setup and signed sync API](server/cloud-vault/README.md)

## Лицензия

MIT - [LICENSE](LICENSE).
