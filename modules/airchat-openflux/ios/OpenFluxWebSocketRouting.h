/*
 * Третий слой перехвата — веб-сокеты React Native.
 *
 * Зачем он появился. Слой 1 (системный прокси Network.framework) и слой 2
 * (конфигурация сессии React Native) вместе покрывают HTTP приложения, и это
 * измерено: соединения к vault уходят в локальный SOCKS5. Веб-сокет ntfy —
 * главный канал AirChat — не покрывал ни один из них. В логе это выглядело так:
 *
 *     [C7 … ntfy.sh:443 tcp, … , no proxy, prohibit fallback] start
 *
 * «no proxy» здесь не про то, что прокси не нашёлся, а про то, что соединение
 * про него вообще не спрашивало. SocketRocket строит поток сам, через CFStream,
 * и параметры этого потока не наследуют ни прокси сессии React Native, ни
 * системный privacy-context процесса. Пока это так, туннель на iOS бесполезен:
 * в сети с белым списком приложение показывает «канал поднят» и не доставляет
 * ни одного сообщения.
 *
 * ── Почему поток SocketRocket не удалось настроить на месте ─────────────────
 *
 * Напрашивалось поставить потоку kCFStreamPropertySOCKSProxy — свойство
 * существует ровно для этого, и SocketRocket его даже умеет: в
 * ReactNativeDependencies.xcframework видно, что SRProxyConnect импортирует
 * kCFProxyTypeSOCKS и всё семейство NSStreamSOCKSProxy*Key. Но узнаёт он про
 * прокси одним-единственным способом —
 *
 *     CFNetworkCopySystemProxySettings() → CFNetworkCopyProxiesForURL()
 *
 * то есть из настроек прокси Wi-Fi, которые приложение на iOS выставить не
 * может. Своего входа туда нет: SocketRocket приезжает прибинаренным, потоки
 * создаёт и открывает приватный класс SRProxyConnect внутри -_configureProxy, а
 * заголовок SRWebSocket.h до них не пускает. К моменту, когда провайдер
 * возвращает объект, потоков ещё нет; к моменту, когда они есть, они уже
 * подключены. Оставались бы подмена приватного метода чужого бинарника или
 * перехват CFNetworkCopySystemProxySettings — ровно та хрупкость, от которой
 * OpenFluxRouting отказывается в своём заголовке.
 *
 * ── Что сделано вместо ──────────────────────────────────────────────────────
 *
 * RCTSetCustomSRWebSocketProvider (React/CoreModules/RCTWebSocketModule.h):
 * React Native спрашивает провайдер на КАЖДОМ открытии сокета и ждёт обратно
 * конкретный SRWebSocket *, а не протокол. Поэтому наш сокет — наследник
 * SRWebSocket ради типа, но ни одной строки его машинерии не использует:
 * внутри NSURLSessionWebSocketTask с proxyConfigurations на конфигурации
 * сессии. Это тот же механизм, которым уже работает слой 2, — про него
 * известно, что он доносит трафик до SOCKS5, а не предположено.
 *
 * ── Что это даёт при переключении на лету ───────────────────────────────────
 *
 * Провайдер спрашивают на каждое соединение, поэтому решение принимается в
 * момент открытия сокета, а не при установке слоя. Туннель опущен — провайдер
 * возвращает nil, React Native строит обычный SRWebSocket, и поведение
 * совпадает с прежним ровно потому, что наш код в этом случае не участвует
 * вовсе. Туннель поднят — сокет идёт через SOCKS5. Уже открытый сокет прокси не
 * подхватит, но его и так закрывают: переключатель в настройках перезапускает
 * интернет-транспорт (OpenFluxSettingsSection.restartTransport).
 */
#import <Foundation/Foundation.h>

NS_ASSUME_NONNULL_BEGIN

/**
 * Где сейчас слушает SOCKS5 ядра, в виде "host:port", или nil, если туннель
 * опущен. Блок зовут на каждое открытие веб-сокета, поэтому отвечать он обязан
 * быстро и из любого потока.
 */
typedef NSString *_Nullable (^AirChatOpenFluxSocksEndpointProvider)(void);

/**
 * Ставит перехват веб-сокетов. Вызывать один раз, вместе со слоем 2.
 *
 * __BEGIN_DECLS обязателен по той же причине, что и в OpenFluxHTTPRouting.h:
 * реализация лежит в .mm (RCTWebSocketModule.h тянет C++), и без обёртки
 * компилятор украсил бы имя по правилам C++, а Swift ищет чистый C-символ.
 */
__BEGIN_DECLS
void AirChatOpenFluxInstallWebSocketProvider(AirChatOpenFluxSocksEndpointProvider provider);
__END_DECLS

NS_ASSUME_NONNULL_END
