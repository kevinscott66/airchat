/**
 * Монотонная отметка времени для служебных конвертов переписки и групп.
 *
 * v4.32.615. Задача: не дать повтору старого кадра откатить настройку.
 *
 * Окно приёма конверта — тридцать суток (`ENVELOPE_MAX_AGE_MS` в messaging.ts,
 * ровно срок хранения на relay). Всё это время кадр остаётся валидным: он
 * подписан, он не протух, и повторно принять его мешает только `seenMessageIds`
 * — множество в памяти, которое обнуляется при перезапуске приложения. У
 * служебных конвертов нет и второй линии: они возвращают управление раньше,
 * чем в базе появится строка с их `messageId`, поэтому `chatMessageExists` их
 * тоже не ловит. Темы relay выводятся из открытых DID, писать в них может
 * любой — значит перехваченный кадр можно послать заново хоть через месяц.
 *
 * Последствия предметные: снятый запрет копирования включается обратно,
 * «последний раз в сети» снова прячется, автоудаление возвращается на старый
 * срок, снятое закрепление возвращается.
 *
 * Лечение то же, что уже применено к профилю контакта (contacts.ts:650) —
 * сравнение с предыдущим `ts`. Здесь оно вынесено в общий модуль, потому что
 * состояний четыре и у каждого свой обработчик.
 *
 * Тем же приёмом закрыты управляющие конверты группы — см. `acceptGroupControlTs`.
 *
 * Отметка хранится через profileScopedKv, а не через голые kvGet/kvSet: те не
 * разделены по профилям (обычный `SELECT v FROM kv WHERE k = ?`), и аккаунты
 * делили бы один водяной знак. Заодно ключ попадает под уборку `p<id>:%` при
 * удалении профиля и отличает «не читается база» от «ещё ничего не было».
 */
import { scopedKvSetCheckedFor, scopedKvTryGetFor } from '../storage/profileScopedKv';
import { READ_RETRY_ATTEMPTS, readRetryDelayMs } from '../storage/readRetry';
import { log } from '../logger';

export const WATERMARK_PREFIX = 'ctl_ts_v1:';

/**
 * Тот же допуск на расхождение часов, что и у внешней проверки конверта
 * (`ENVELOPE_MAX_SKEW_MS` в messaging.ts). Держать его здесь отдельной
 * константой намеренно: модуль не должен зависеть от службы переписки —
 * иначе получится цикл импортов через контроль-фанаут.
 */
export const CONTROL_TS_MAX_SKEW_MS = 5 * 60 * 1000;

/**
 * Виды состояний, у каждого свой водяной знак.
 *
 * Здесь только СКАЛЯРНЫЕ состояния пары: одно значение на собеседника. Для
 * них «пришло старее уже применённого» и означает откат — отвергнуть верно.
 *
 * Закрепления в личке и реакции сюда намеренно не входят: их состояние
 * привязано к конкретному сообщению, и один водяной знак на собеседника
 * выбросил бы законное закрепление сообщения B, если конверт про A с большей
 * меткой пришёл раньше (relay отдаёт накопленное пачкой, порядок не гарантирован).
 * Им нужна отметка на элемент, а не на пару. Для закреплений она сделана
 * (v4.32.793, {@link dmPinWatermarkKey}) — ключ включает и сообщение; остаются
 * реакции, где состояние не скалярное вовсе, и разбирать его нужно иначе.
 *
 * `dmpin_clear` — исключение из этого исключения и потому здесь (v4.32.622):
 * «открепить всё» не называет сообщения, оно относится ко всей паре и потому
 * скалярно. Своя ячейка, отдельная от закреплений отдельных сообщений: она их
 * не отвергает и ими не двигается. Без неё повтор одного такого конверта
 * (relay хранит накопленное 30 суток) стирал весь список закреплений заново.
 */
export type ControlKind = 'disappear' | 'copyguard' | 'presence' | 'dmpin_clear';

/** Имя ключа ДО добавления префикса профиля (его дописывает profileScopedKv). */
export function watermarkKey(kind: ControlKind, peerPubB64: string): string {
  return `${WATERMARK_PREFIX}${kind}:${peerPubB64}`;
}

/**
 * Свежее ли решение служебного конверта. Отметку НЕ двигает.
 *
 * Возвращает true, если конверт свежее всего, что уже применено к этой паре
 * (профиль, вид состояния, собеседник). false — если это повтор, откат или
 * заведомо испорченная метка.
 *
 * Метку из будущего дальше допуска на часы отвергаем: честный отправитель
 * такую не поставит (внешняя проверка конверта отсекает его же по
 * `em.timestamp` с тем же допуском), а принять её значило бы навсегда закрыть
 * приём — все последующие законные конверты оказались бы «старыми».
 *
 * Ошибка базы больше не означает «пропустить» (v4.32.791): применённое помнит
 * зеркало в памяти процесса, и по нему повтор отбивается даже тогда, когда знак
 * не лёг на диск или не прочитался обратно. Прежний мягкий ответ остался лишь
 * там, где сказать нечего вовсе, — пустое зеркало и нечитаемая база разом.
 *
 * Пара к ней — {@link commitControlTs}, и порядок между ними обязателен:
 * проверить свежесть → применить → сдвинуть. Слитная форма «проверить и сразу
 * сдвинуть» жила тут до v4.32.778 и была ровно той миной, которую с v4.32.655
 * по v4.32.777 разминировали по одному приёмнику: применение умеет не удаться,
 * а знак уже стоит — и повтор того же конверта отвергается как старый. Повтора
 * же у служебного конверта нет вовсе, так что отказ становился вечным. Из
 * приложения слитную форму убрали целиком, чтобы её некому было позвать снова.
 */
export async function controlTsFresh(
  kind: ControlKind,
  peerPubB64: string,
  pid: number,
  ts: number
): Promise<boolean> {
  return freshTs(watermarkKey(kind, peerPubB64), kind, pid, ts);
}

/** Сдвинуть отметку личной переписки — после того, как изменение применено. */
export async function commitControlTs(
  kind: ControlKind,
  peerPubB64: string,
  pid: number,
  ts: number
): Promise<void> {
  await commitTs(watermarkKey(kind, peerPubB64), kind, pid, ts);
}

/**
 * Слоты водяного знака внутри группы (v4.32.615).
 *
 * `m:<ключ участника>` — состав и роль ЭТОГО человека: один скаляр, который
 * меняют ban, unban, kick, add, role и «вышел сам». Все они спорят за одно
 * значение, поэтому и знак у них общий: пришедшее старее применённого — откат.
 *
 * `meta:<поле>` — по знаку НА КАЖДОЕ поле настроек, а не один на группу.
 * Настройки группы — не скаляр, их десяток и меняют их независимо; relay
 * отдаёт накопленное пачкой без гарантии порядка, и общий знак выбросил бы
 * законное переименование, пришедшее следом за более поздней сменой аватара.
 */
export type GroupControlSlot = `m:${string}` | `meta:${string}`;

/**
 * Идентификатор группы идёт ПОСЛЕДНИМ, и это не косметика: кодек ограничивает
 * его только длиной (128 символов), двоеточие в нём допустимо. Стой он в
 * середине — группа с именем вида «A:m:<ключ>» подобрала бы себе ключ чужого
 * слота. Слот же формы не выбирает: это либо `m:` с ключом в base64 (двоеточий
 * не бывает), либо `meta:` с полем из закрытого списка.
 */
export function groupWatermarkKey(slot: GroupControlSlot, groupId: string): string {
  return `${WATERMARK_PREFIX}grp:${slot}:${groupId}`;
}

/**
 * Свежее ли решение управляющего конверта группы. Отметку НЕ двигает
 * (v4.32.618).
 *
 * Повтор здесь стоит дороже, чем в личной переписке: конверт применяется от
 * имени того, кто подписал ОРИГИНАЛ, а его права проверяются по текущему
 * составу. Разжалованный администратор, чей конверт «назначить админом» кто-то
 * перехватил, возвращает себе права у каждого участника — отправитель-то
 * остался администратором. Тем же приёмом снимается бан, возвращается
 * исключённый, отменяется отзыв пригласительной ссылки и включается обратно
 * автоудаление переписки.
 *
 * Хуже того, повтор проходил молча: идентификатор системной строки собирается
 * из `ts` операции, при повторе он тот же, INSERT OR IGNORE ничего не пишет —
 * роль менялась, а строки «X назначен(а) администратором» на экране не было.
 *
 * Нужна там, где решение «применить» принимается уже после проверки прав и
 * может кончиться ничем: ban по уже забаненному, role с той же ролью, add по
 * уже состоящему — все эти ветки возвращают управление, ничего не изменив.
 * Сдвинутая отметка при этом оставалась бы навсегда, и следующий конверт с
 * меньшей меткой отвергался бы как повтор, хотя применить его было надо.
 *
 * Пример, который это ломало: `add` (ts=T1) и `role` (ts=T2 > T1) пришли
 * пачкой в обратном порядке. `role` не нашёл участника и вышел, но знак уже
 * стоял на T2 — и `add` с T1 отвергался. Человек исчезал из группы навсегда.
 *
 * Пара к ней — `commitGroupControlTs`, её вызывают ровно тогда, когда
 * изменение действительно применено.
 */
export async function groupControlTsFresh(
  slot: GroupControlSlot,
  groupId: string,
  pid: number,
  ts: number
): Promise<boolean> {
  return freshTs(groupWatermarkKey(slot, groupId), groupKindLabel(slot), pid, ts);
}

/** Сдвинуть отметку слота вперёд — после того, как изменение применено. */
export async function commitGroupControlTs(
  slot: GroupControlSlot,
  groupId: string,
  pid: number,
  ts: number
): Promise<void> {
  await commitTs(groupWatermarkKey(slot, groupId), groupKindLabel(slot), pid, ts);
}

/**
 * Слот ОДНОГО сообщения группы — правка и удаление (v4.32.628).
 *
 * Стоит отдельно от {@link groupWatermarkKey}, и идентификатор сообщения здесь
 * последний по той же причине, по которой там последним стоит идентификатор
 * группы: кодек ограничивает и то и другое только длиной, двоеточие внутри
 * допустимо. Две неограниченные части в одном имени подобрали бы друг другу
 * чужую ячейку, поэтому имя группы сюда не входит вовсе — оно и не нужно:
 * сообщение уже найдено в своей группе (`getGroupMessageTarget`), а
 * идентификатор сообщения в базе уникален сам по себе.
 *
 * Знак именно на сообщение, а не на отправителя: правки разных сообщений
 * приходят пачкой без гарантии порядка, и общий знак выбросил бы законную
 * правку сообщения A, пришедшую следом за более поздней правкой B.
 */
export function groupMessageWatermarkKey(msgId: string): string {
  return `${WATERMARK_PREFIX}grp:msg:${msgId}`;
}

/** Свежесть правки сообщения — без сдвига отметки (см. groupControlTsFresh). */
export async function groupMessageTsFresh(msgId: string, pid: number, ts: number): Promise<boolean> {
  return freshTs(groupMessageWatermarkKey(msgId), 'grp:msg', pid, ts);
}

/** Сдвинуть отметку сообщения — после того, как правка или удаление применены. */
export async function commitGroupMessageTs(msgId: string, pid: number, ts: number): Promise<void> {
  await commitTs(groupMessageWatermarkKey(msgId), 'grp:msg', pid, ts);
}

/**
 * Слот ЗАКРЕПЛЕНИЯ одного сообщения группы (v4.32.792).
 *
 * До этой версии у закрепления не было знака вовсе, и это было записано прямо
 * в коде обработчика как данность. Между тем состояние скалярное — у пары
 * «группа + сообщение» ровно два положения, `on` и `off`, — и повтор кадра его
 * переключает: сохранённый `pin:on` возвращает в шапку снятый баннер, а
 * сохранённый `pin:off` снимает нынешний. Право при этом проверяется по
 * ТЕКУЩЕМУ составу и подписанту оригинала, так что участник, у которого
 * закрепление отобрали настройкой `adminOnlyPinning`, всё равно проходит, если
 * его старый кадр был перехвачен.
 *
 * Ячейка своя, отдельная от `grp:msg:` (правка и удаление того же сообщения):
 * закрепление и правка спорят за разные значения, и общий знак выбросил бы
 * законную правку, пришедшую следом за более поздним закреплением. Порядок
 * между ними relay не держит — накопленное отдаётся пачкой.
 *
 * Идентификатор сообщения последний по той же причине, что и в соседних
 * ключах: кодек ограничивает его только длиной, двоеточия внутри допустимы.
 */
export function groupPinWatermarkKey(msgId: string): string {
  return `${WATERMARK_PREFIX}grp:pin:${msgId}`;
}

/** Свежесть закрепления — без сдвига отметки (см. groupControlTsFresh). */
export async function groupPinTsFresh(msgId: string, pid: number, ts: number): Promise<boolean> {
  return freshTs(groupPinWatermarkKey(msgId), 'grp:pin', pid, ts);
}

/** Сдвинуть отметку закрепления — после того, как оно применено и рассказано. */
export async function commitGroupPinTs(msgId: string, pid: number, ts: number): Promise<void> {
  await commitTs(groupPinWatermarkKey(msgId), 'grp:pin', pid, ts);
}

/**
 * Слот ЗАКРЕПЛЕНИЯ одного сообщения в личной переписке (v4.32.793).
 *
 * Та же дыра, что закрыта в группах версией раньше, только в личке: у пары
 * «собеседник + сообщение» два положения, кадр называет своё сам, и повтор его
 * переключает. Конверт закрепления не создаёт в переписке никакой строки —
 * человек не видит ни самого события, ни его отката, — а relay хранит
 * накопленное тридцать суток и тему выводит из открытых DID. Сохранённое
 * «закрепить» возвращает в шапку снятый баннер, сохранённое «открепить»
 * снимает нынешний.
 *
 * Ячейка на СООБЩЕНИЕ, а не на собеседника: закрепления разных сообщений
 * приходят пачкой без гарантии порядка, и общий знак выбросил бы законное
 * закрепление сообщения A, пришедшее следом за более поздним про B. Ровно об
 * этом говорит оговорка у {@link ControlKind} — вот она и отработана.
 *
 * Собеседник в имени всё же есть, и стоит он перед идентификатором сообщения:
 * чат определяется подписанным отправителем, но идентификатор в кадре — поле,
 * и без разделения контакт A двигал бы знак закрепления в переписке с B.
 * Открытый ключ в base64 фиксированной длины и двоеточий не содержит, а вот
 * идентификатор сообщения ограничен только длиной — потому он и последний.
 */
export function dmPinWatermarkKey(peerPubB64: string, msgId: string): string {
  return `${WATERMARK_PREFIX}dm:pin:${peerPubB64}:${msgId}`;
}

/** Свежесть закрепления в личке — без сдвига отметки (см. controlTsFresh). */
export async function dmPinTsFresh(
  peerPubB64: string,
  msgId: string,
  pid: number,
  ts: number
): Promise<boolean> {
  return freshTs(dmPinWatermarkKey(peerPubB64, msgId), 'dm:pin', pid, ts);
}

/** Сдвинуть отметку закрепления в личке — после того, как оно применено. */
export async function commitDmPinTs(
  peerPubB64: string,
  msgId: string,
  pid: number,
  ts: number
): Promise<void> {
  await commitTs(dmPinWatermarkKey(peerPubB64, msgId), 'dm:pin', pid, ts);
}

function groupKindLabel(slot: GroupControlSlot): string {
  return `grp:${slot.split(':')[0]}`;
}


/**
 * Зеркало отметок в памяти процесса (v4.32.791).
 *
 * Знак сторожит необратимое: состав группы, роли, баны, снятие пометки об
 * исключении. А сам он до этой версии терялся дважды.
 *
 * Первое. `commitTs` о неудавшейся записи только писал в журнал: вызывающий об
 * этом не узнавал, и знак оставался там, где стоял до операции. Дальше повтор
 * перехваченного кадра проходил проверку свежести честно — по метке он и
 * правда новее того, что записано. Так возвращались снятые права
 * администратора и снимался бан: подписавший оригинал как был администратором,
 * так и остался, и `canModerate` ему не мешает.
 *
 * Второе. Нечитаемая база отвечала «свежо» на что угодно, и в тридцатисуточное
 * окно проходил любой сохранённый кадр состава.
 *
 * Отсюда зеркало. Оно держит наибольшую отметку, которую этот запуск успел
 * применить, — независимо от того, легла она на диск или нет, — и читающая
 * сторона судит по нему наравне с диском. Память процесса диск не заменяет:
 * перезапуск её не переживёт. Но заминка базы длится секунды, а повтор
 * приходит тогда, когда его пришлют, — чаще всего в ту же сессию. Тот же
 * приём, что у пометок выхода и исключения (v4.32.787, markFallback);
 * отдельная карта здесь потому, что здесь нужна не «не легло», а «наибольшее
 * из применённого».
 *
 * Потолок задан: ключи приходят снаружи (слот на каждого участника каждой
 * группы, на каждое правимое сообщение), считать их некому. Вытесняется самая
 * давняя — для неё всё становится ровно так, как было до этой версии.
 */
const MIRROR_LIMIT = 4096;
const mirror = new Map<string, number>();

const mirrorId = (pid: number, key: string): string => `${pid}\u0000${key}`;

/** Запомнить применённое. Отметка только растёт: откат её не двигает. */
function mirrorRemember(pid: number, key: string, ts: number): void {
  const id = mirrorId(pid, key);
  const next = Math.max(mirror.get(id) ?? 0, Math.floor(ts));
  // Переставляем в конец: порядок вставки — это и есть порядок вытеснения.
  mirror.delete(id);
  mirror.set(id, next);
  while (mirror.size > MIRROR_LIMIT) {
    const oldest = mirror.keys().next();
    if (oldest.done) break;
    mirror.delete(oldest.value);
  }
}

/** Наибольшее применённое этим запуском, либо 0 — «не знаем». */
function mirrorGet(pid: number, key: string): number {
  return mirror.get(mirrorId(pid, key)) ?? 0;
}

/** Только для проверок: сколько слотов держится в памяти. */
export function controlTsMirrorSize(): number {
  return mirror.size;
}

/**
 * Только для тестов: очистить зеркало.
 *
 * Зеркало живёт на уровне модуля, как и положено памяти процесса, — а набор
 * проверок в одном файле делит один модуль. Без этой уборки применённое в
 * одной проверке судило бы конверты следующей.
 */
export function resetControlTsMirrorForTests(): void {
  mirror.clear();
}

/**
 * Прочитать знак с повтором по занятой секунде (v4.32.791).
 *
 * Те же паузы, что у прочих чтений и у журнала звонков (v4.32.749). Повтор
 * стоит дёшево — служебные конверты приходят поштучно, не потоком, — а платит
 * за него тот единственный случай, когда база занята соседним запросом ровно в
 * миг, когда решается судьба состава группы.
 */
async function readWatermark(pid: number, key: string): Promise<{ value: string | null } | null> {
  for (let attempt = 0; ; attempt++) {
    const got = await scopedKvTryGetFor(pid, key);
    if (got !== null || attempt >= READ_RETRY_ATTEMPTS) return got;
    await new Promise((r) => setTimeout(r, readRetryDelayMs(attempt + 1)));
  }
}

async function freshTs(key: string, kind: string, pid: number, ts: number): Promise<boolean> {
  if (!Number.isFinite(ts) || ts <= 0) {
    log.warn('control_ts_malformed', { kind, ts });
    return false;
  }
  if (ts > Date.now() + CONTROL_TS_MAX_SKEW_MS) {
    log.warn('control_ts_future', { kind, ts });
    return false;
  }
  const remembered = mirrorGet(pid, key);
  let prev = remembered;
  let readOk = false;
  try {
    const got = await readWatermark(pid, key);
    if (got === null) {
      log.warn('control_ts_read_failed', { kind });
    } else {
      readOk = true;
      const n = got.value === null ? NaN : Number(got.value);
      if (Number.isFinite(n) && n > prev) prev = n;
    }
  } catch (e) {
    log.warn('control_ts_read_failed', { kind, err: e instanceof Error ? e.message : String(e) });
  }
  // Диск не прочитался, и в памяти о слоте ничего нет: судить не по чему.
  // Здесь и только здесь остаётся прежний ответ «пропустить» (см. заголовок
  // файла): приложение без доступа к базе не должно молча переставать
  // применять решения собеседника. Окно это узкое — оно требует и перезапуска,
  // и неработающей kv в ту же секунду.
  if (!readOk && remembered === 0) {
    log.warn('control_ts_unknown_pass', { kind });
    return true;
  }
  if (ts <= prev) {
    log.warn('control_ts_replay_rejected', { kind, ts, prev });
    return false;
  }
  return true;
}

/**
 * Сдвиг отметки. Пишет проверенной формой (v4.32.655).
 *
 * Раньше здесь стоял scopedKvSetFor внутри try/catch. Та форма отдаёт void и
 * гасит отказ базы внутри, так что catch не срабатывал ни разу и строки
 * control_ts_write_failed не было в журнале никогда: неудавшийся сдвиг
 * выглядел точно как удавшийся. С v4.32.655 причина видна, а с v4.32.791
 * не легший сдвиг ещё и перестал быть дорогой для повтора: зеркало помнит его
 * и без диска.
 */
async function commitTs(key: string, kind: string, pid: number, ts: number): Promise<void> {
  const at = Math.floor(ts);
  // v4.32.791: зеркало ставится ДО записи и остаётся стоять, даже если запись
  // не удалась. Именно на не легший сдвиг и рассчитан повтор: по диску кадр
  // выглядит новее применённого, а по памяти — нет.
  mirrorRemember(pid, key, at);
  if (!(await scopedKvSetCheckedFor(pid, key, String(at)))) {
    log.warn('control_ts_write_failed', { kind });
  }
}
