/**
 * Общий ограничитель расшифровки вложений.
 *
 * v4.32.248: каждое незакэшированное вложение — это загрузка до 8 МБ. Пока
 * картинок было столько, сколько помещается на экран, очередь была не нужна;
 * с появлением галереи «Общие медиа» список стал сотнями плиток, и без предела
 * её открытие означало бы сотни одновременных запросов и забитый канал на всё
 * время просмотра.
 *
 * v4.32.359: ограничитель один на приложение — и для отрисовки чата, и для
 * разбора по требованию (одноразовое фото). Два независимых предела по четыре
 * давали бы восемь: сам смысл ограничения в том, что оно общее для канала.
 */

import { createSemaphore } from '../utils/semaphore';

export const MAX_PARALLEL_RESOLVES = 4;

export const mediaResolveLimiter = createSemaphore(MAX_PARALLEL_RESOLVES);
