/**
 * officialKeys — открытые ключи, чьей подписи достаточно для галочки.
 *
 * Отдельным модулем, а не константой внутри `verification`, по двум причинам.
 *
 * Первая: это единственное место во всём приложении, которому доверяют «на
 * веру». Всё остальное в проверке — арифметика: подпись либо сходится, либо
 * нет. Список ключей ничем не проверяется и проверен быть не может, он и есть
 * корень доверия. Такое стоит держать на виду и в одиночестве, чтобы правка
 * этого файла в diff'е бросалась в глаза, а не терялась среди правок разбора.
 *
 * Вторая: подписать бумагу настоящим ключом тест не может — закрытая половина
 * в репозитории не лежит и лежать не будет. Отдельный модуль тест подменяет
 * целиком (`jest.mock`), и проверка положительного случая обходится без
 * параметра «а каким ключом проверять», который в бою не нужен никому и лишь
 * расширял бы поверхность доверия.
 *
 * Список, а не одно значение: смена ключа обязана быть возможной без того,
 * чтобы все выданные раньше бумаги в один день перестали проверяться. Новый
 * ключ дописывается в начало, старый уезжает из списка следующей версией —
 * когда все выданные под него бумаги перевыпущены.
 */
export const OFFICIAL_VERIFIER_KEYS: readonly string[] = [
  'mhew8W95ET6owjdkGJFFtcDUu9JZNk11gJYpeTOopDM=',
];
