/**
 * Нативная заглушка: кольцо фокуса на ios/android рисует система, а не мы.
 *
 * Файл существует ради платформенного расширения `.web.ts` — Metro подставляет
 * веб-вариант, а здесь остаётся no-op, чтобы entry импортировал одно имя.
 */
export function installWebFocusRing(): void {}
