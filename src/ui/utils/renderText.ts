/**
 * Правила показа чужого текста в списках сообщений (v4.32.327).
 *
 * Сама вычистка недоверенной строки живёт в core/social/sysLineGuard рядом с
 * остальными правилами обращения с чужим текстом (переехала туда в v4.32.328,
 * когда она понадобилась ещё и списку диалогов). Здесь остаётся то, что имеет
 * смысл только при отрисовке.
 */
export { sanitizeBodyForRender } from '../../core/social/sysLineGuard';

/**
 * Сколько кусочков разметки готовы нарисовать, прежде чем показать текст
 * простым.
 *
 * Разбор возвращает по элементу `<Text>` на каждый кусок, а RN раскладывает их
 * все, даже когда виден один абзац. В обычном сообщении таких кусков единицы;
 * шестьдесят килобайт из «_a_», повторённого до предела длины, дают их
 * тридцать тысяч — и это уже не сообщение, а способ подвесить чужой список
 * чатов. Выше предела текст показывается без разметки: он остаётся читаемым,
 * а нарисовать его стоит одного элемента.
 */
export const MAX_RENDER_SEGMENTS = 300;
