/**
 * Режим полосы публикаций: три разных вопроса к одному автору.
 *
 * Вынесен из `ProfilePostsModal` отдельно (v4.32.600), чтобы подписи под
 * пустыми разделами могли ссылаться на тип, не втягивая за собой сам экран с
 * его react-native-импортами.
 */
export type ProfilePostsMode = 'wall' | 'stories' | 'archive';
