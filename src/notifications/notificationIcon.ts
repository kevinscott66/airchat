/**
 * notificationIcon — одно имя ресурса small icon на весь Android-путь уведомлений.
 *
 * v4.32.517. Дефект, который этот модуль закрывает, стоил приложению всех
 * уведомлений разом. Имя 'ic_notification' было выписано строковым литералом в
 * пяти местах, а ресурса с таким именем в проекте не было; шестое место
 * (напоминания) small icon не задавало вовсе. notifee ищет имя в mipmap и
 * drawable, не находит, молча оставляет small icon пустым — а Android на
 * уведомление без small icon отвечает IllegalArgumentException. Каждый из
 * шести вызовов был обёрнут в catch, поэтому отказ выглядел как тишина.
 *
 * Литерал не должен возвращаться в вызовы: имя ресурса — это связь с файлом
 * assets/android/res/drawable/ic_notification.xml, и проверять её нужно в
 * одном месте, а не искать шесть строк глазами. Исходник ресурса лежит в
 * assets/, потому что каталог android/ целиком генерируется expo prebuild и
 * находится в .gitignore; в сборку файл кладёт
 * scripts/post-prebuild-android-patches.sh. Тест рядом с модулем держит все
 * стороны связи: что константа равна имени файла, что файл на месте, что
 * литерала больше нет ни в одном исходнике и что перенос в сборку описан.
 */

/** Имя drawable-ресурса. Должно совпадать с именем файла в res/drawable. */
export const NOTIFICATION_SMALL_ICON = 'ic_notification';
